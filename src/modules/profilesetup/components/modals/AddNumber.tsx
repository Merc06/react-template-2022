import React from 'react';

const AddNumber = () => {
    return (
        <>test</>
        // <Modal
        //     isOpen={isModalOpen}
        //     title={`${_.isEmpty(itemToEdit) ? 'ADD' : 'EDIT'} ${type}`}
        // >
        //     test
        // </Modal>
    );
};

export default AddNumber;