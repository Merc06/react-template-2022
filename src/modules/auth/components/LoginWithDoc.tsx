import React from 'react';
import Button from '../../common/components/Button';

const LoginWithDoc = () => {
    return (
        <Button
            className='uppercase text-white bg-grayblack font-bold w-full'
        >
            Login with Doconchain Account
        </Button>
    );
};

export default LoginWithDoc;